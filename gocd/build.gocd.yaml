format_version: 4
pipelines:
  "document-store-prototype":
    group: document-store
    label_template: "${git[:8]}"
    materials:
      git:
        type: configrepo
        blacklist:
          - .git*
          - .idea/**/*.*
          - documentation/**/*.*
          - gradle/**/*.*
    stages:
      - build:
          clean_workspace: true
          jobs:
            build:
              artifacts:
                - build:
                    source: app/build/libs
                    destination: jars
                - test:
                    source: app/build/test-results
                    destination: test-reports
              tasks:
                - exec:
                    command: dojo
                    arguments:
                      - "./gradlew app:build"
      - deploy:
          clean_workspace: true
          jobs:
            terraform:
              artifacts:
                - build:
                    source: api_gateway_url_artifact
                    destination: properties
              tasks:
                - fetch:
                    stage: build
                    job: build
                    source: jars
                - exec:
                    command: dojo
                    arguments:
                      - -c
                      - Dojofile-infra
                      - 'gocd/tasks.sh plan-deploy'
                - exec:
                    command: dojo
                    arguments:
                      - -c
                      - Dojofile-infra
                      - 'gocd/tasks.sh deploy'
                - exec:
                    command: dojo
                    arguments:
                      - -c
                      - Dojofile-infra
                      - 'gocd/tasks.sh extract-api-url'
      - testHarness:
          clean_workspace: true
          jobs:
            runTestHarness:
              environment_variables:
                API_AUTH: IAM
              tasks:
                - fetch:
                    stage: deploy
                    job: terraform
                    source: properties
                - exec:
                    command: dojo
                    arguments:
                      - "cat properties/api_gateway_url_artifact"
                - exec:
                    command: dojo
                    arguments:
                      - "ls -lsa"
                - exec:
                    command: dojo
                    arguments:
                      - "DOCUMENT_STORE_BASE_URI=$(cat properties/api_gateway_url_artifact) ./gradlew testHarness:test"

format_version: 4
pipelines:
  access-request-fulfillment.audit:
    group: paperless-record-service
    label_template: '${git[:8]}'
    materials:
      git:
        type: configrepo
        blacklist:
          - documentation/*
    timer:
      spec: 0 0 7 ? * MON,TUE,WED,THU,FRI *
    stages:
      - audit:
          clean_workspace: true
          approval: manual
          jobs:
            dependency_check:
              artifacts:
                - build:
                    source: app/build/reports
              tabs:
                dependency: reports/dependency-check-report.html
              resources:
                - docker
              tasks:
                - exec:
                    command: /bin/bash
                    arguments:
                      - -c
                      - ./tasks java-dependency-scan
                    
